mode: ContinuousDeployment
tag-prefix: '[Rr]eleases?[/-]'
branches:
    master:
        increment: None
    release:
        regex: releases?[/-]
        mode: ContinuousDelivery
        increment: None
        tag: '' 
ignore:
  sha: []
